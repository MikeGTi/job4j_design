create table engines(id serial primary key,
                      manufacturer varchar(255),
                      model varchar(255),
                      volume double precision,
                      power integer);